[package]
name = "module_bindings"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
# The bindings generated by `spacetime generate` produce a library at
# `module_bindings/src/lib.rs` which is re-exported by this crate.
path = "src/lib.rs"

[dependencies]
spacetimedb-sdk = { workspace = true }
